# AGENTS.md — 目录与约束规范（强约束）

本文件是本仓库的**强约束规范**。任何 Agent 在本仓库内工作前必须先读本文件；其规则
优先级高于任何便捷性判断。它固定目录结构、分类规则、分层上下文路由规则与文档写作规则。

## 一、仓库定位与三分区

本仓库承载本机对 DeepSeek Harness（DSH）的定制、扩展与内置知识库，三个分区：

| 分区 | 放什么 | 判定标准 |
| --- | --- | --- |
| `plugins/manual/` | 手动维护的插件 | **我们自己手写的**代码/补丁/脚本，source of truth 在本仓库 |
| `plugins/community/` | 三方社区插件 | `dsh plugin add` 装的 npm 包，本仓库**只留集成配方/记录，绝不 vendor 源码** |
| `knowledge/` | 内置知识库 | 关于 DSH 机制本身、领域知识或操作步骤，**跨插件通用** |

边界示例（必须照此归类）：

- `web-remote-access`（自己写的 `cordis.patch.yml` + launchd 脚本）→ `manual/`
- `modlens-vision`（modlens 是 npm 三方包，仓库只留集成配方 + 基准）→ `community/`
- 「DSH 配置面是 loopback-only」（DSH 自身的机制事实）→ `knowledge/foundations/`

## 二、铁律：目录树即索引，禁止维护清单

1. **唯一索引是目录树本身**。要"列出所有插件/知识"，直接 `ls` 对应目录。
2. **禁止**在父级 README 或任何地方维护"模块/能力/状态"这类集中式表格/清单——
   这类清单每次增删都要改，必然腐化。
3. **每个目录必须有 `README.md`**，且必须回答两件事：①这里放什么（判定标准）②什么时候读它。
4. **加一个模块 = 新建一个目录 + 写它的 README**。绝不改动父级 README 的内容清单。
   父级 README 只承载"结构与路由规则"，不承载"内容罗列"。

公开首页有一个窄例外：根 `README.md` 与语言镜像可维护**非穷举、最多四项**的旗舰能力展示，用于
项目定位与演示；它不得包含全量模块表、状态矩阵或 roadmap，并必须明确把目录树作为完整索引。
`CHANGELOG.md` 可记录按日期的仓库级里程碑，`ACKNOWLEDGEMENTS.md` 可按归属关系列出第三方项目；
两者都不属于模块索引。

## 三、分层上下文路由规则

知识库按层组织，路由靠目录名 + README，不靠清单：

| 层 | 目录 | 何时读 |
| --- | --- | --- |
| L0 基础事实 | `knowledge/foundations/` | 涉及 DSH 机制时**必读**；文件名即主题 |
| L1 领域知识 | `knowledge/domains/<领域>/` | 任务匹配该领域时读 |
| L2 操作手册 | `knowledge/runbooks/<操作>/` | 执行具体操作时读 |

任务开始时的上下文定位顺序：

1. 读本仓库 `README.md`（1 分钟了解结构）。
2. 判定任务属于哪个分区/层。
3. 按需 descend：先读目标目录的 `README.md`，再读具体文件。
4. 涉及 DSH 机制 → 先查 `knowledge/foundations/`（有就是权威事实，直接引用）。

## 四、知识库文档写作规则

- 每个知识文档**独立自足**：一段话讲清「是什么 / 为什么」，再给证据
  （源码路径 + 行号、实测命令输出、官方文档原文三者至少其一）。
- 文件名英文 kebab-case，主题自明（如 `loopback-only-config-surface.md`）。
- **一段事实只写一次**（single source of truth）；别处需要就链接引用，不复制粘贴。
- 文档内不维护"相关链接列表"——相关性由目录树表达。

## 五、模块 README 与命名

- 模块 README 必须含三段：**改了什么 / 怎么生效 / 怎么回滚**（延续本仓库既有约定）。
- 目录/文件名一律英文 kebab-case。
- 回复用户用简体中文；代码注释/文档可中英，优先英文。
- 社区插件模块的 README 至少写清：安装命令（版本写死）、引擎/配置落盘位置、
  验证方式、回滚命令、集成踩坑记录（带证据）。

## 六、公开版迁移规范（强约束）

本仓库最初来自一次人工审阅后的净化快照。后续外部贡献也必须遵守同样的公开发布检查。

1. **迁入内容不得携带私有 Git 历史。** 从私有来源迁移时必须生成经审阅的文件快照；历史提交
   可能保留已删除的域名、路径或凭据。
2. **禁止公开内部标识。** 不得出现公司/团队专用域名、代码托管地址、内部服务名、内部仓库名、
   员工账号、主机名、局域网 IP、设备路径、绝对用户目录、launchd/systemd 私有标识。
3. **禁止公开秘密。** Token、API key、Cookie、Webhook、私钥、登录态、真实配置文件内容
   一律不得进入快照；示例只能引用环境变量或明显的占位符。
4. **外部内容必须三方化。** 内部网关、模型服务、反向代理、通知入口等统一改写为通用的
   OpenAI-compatible provider、third-party API、reverse proxy、webhook 等概念；示例使用
   `https://api.example.com`、`<your-host>`、`<user>`、`$HOME`、`${ENV_NAME}` 等占位符。
5. **不得留下可反推线索。** 不能只遮掉域名却保留内部服务名、模型别名、组织名、路径层级、
   截图账号或真实测试输出；图片、构建产物、source map、基准 JSON 也必须扫描。
6. **公开文档必须自足。** 不能依赖内部知识库、内部仓库或本机安装路径；安装、验证、回滚
   说明都要能由外部用户独立执行。
7. **发布前双重检查。** 至少执行一次内部标识/绝对路径检索和一次秘密扫描，并人工复核全部
   命中项。检查范围包含源码、文档、锁文件、图片元数据、构建产物及待提交的 Git diff。
8. **公开仓库不是私有部署配置的存放处。** 私有配置应留在各自安全环境；需要复用公开修订时
   人工挑选，不得让私有配置反向进入提交。
